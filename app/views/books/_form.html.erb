<%= form_with(model: book) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% book.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

      <%= render(FieldComponent.new(:name, form: form, css_class: %w[rounded-b-none rounded-t-md bg-white], type: :text, fieldheader:'Book name',fieldlabel:'Type book name - this is very important', placeholder: 'Book', autofocus: true, autocomplete: 'email', required: true)) %>
      <%= render(FieldComponent.new(:author, form: form, css_class: %w[rounded-b-none rounded-t-md bg-white], type: :text, fieldheader:'Author',fieldlabel:'Author - this is very important', placeholder: 'Book', autofocus: true, autocomplete: 'email', required: true)) %>
      <%= render(FieldComponent.new(:category, form: form, css_class: %w[rounded-b-none rounded-t-md bg-white w-1], type: :text, fieldheader:'Category',fieldlabel: 'nill', placeholder: 'aa', autofocus: true, autocomplete: 'email', required: true)) %>
      <%= render(FieldComponent.new(:price, form: form, css_class: %w[rounded-b-none rounded-t-md bg-white w-2], type: :number, fieldheader:'Price',fieldlabel:'Please fill the price', placeholder: 'Price', autofocus: true, autocomplete: 'email', required: true)) %>
      <%= render(ButtonComponent.new(text: 'Save')) %>
      
<% end %>
