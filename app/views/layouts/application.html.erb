<!DOCTYPE html>
<html>
  <head>
    <title>RailsHotwireBase</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', data: { turbo_track: :reload } %>
    <%= javascript_pack_tag 'application', data: { turbo_track: :reload } %>
    <%= yield :head %>
    <%= stylesheet_pack_tag "application", data: { turbo_track: :reload } %>
  </head>

    <body>
    <div class="flex flex-row min-h-screen bg-gray-100 text-gray-800" data-controller="nav" data-nav-url="http://localhost:3000/crudhistoryactions">
    <%= render "shared/leftmenu" %>
    <main class="main flex flex-col flex-grow -ml-64 md:ml-0 transition-all duration-150 ease-in">
      <%= render "shared/topbar" %>    
        
      <div class="main-content flex flex-col flex-grow ">
        <div class="flex flex-col flex-grow bg-white rounded mt-1 p-2 ">
       
       <%= yield %>
		  </div>
      </div>
      <%= render "shared/footerbar" %>
    </main>
  </div>
   
  </body>
</html>
